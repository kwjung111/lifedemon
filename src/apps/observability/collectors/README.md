# Collectors

Future source-specific collectors live here. Each collector produces raw `LifeSignal` objects and declares its consent scope, retention needs, and collection cadence.

No collectors are implemented in the skeleton.
